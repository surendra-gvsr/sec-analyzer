# Web framework
fastapi==0.115.4
uvicorn[standard]==0.30.6
python-multipart==0.0.9

# Environment and config
python-dotenv==1.0.1
pydantic==2.9.2
pydantic-settings==2.2.1

# LlamaIndex core (0.12.x required by llama-index-llms-groq 0.3.x)
llama-index-core>=0.12.0,<0.13.0
llama-index-llms-groq>=0.3.1
llama-index-embeddings-fastembed>=0.2.0
llama-index-vector-stores-chroma>=0.2.1
llama-index-retrievers-bm25==0.5.2   # BM25 keyword retriever — pinned for 0.12.x compat

# LlamaParse cloud API
llama-parse>=0.5.19

# Vector store
chromadb==0.5.20

# Graph backend (used by SimplePropertyGraphStore)
networkx==3.3

# SEC EDGAR download — using direct HTTP via httpx (no dependency)

# LLM provider (Groq)
groq==0.11.0

# Lightweight embeddings (no torch required)
fastembed>=0.3.6

# Lightweight cross-encoder reranker (ONNX, no torch)
flashrank>=0.2.0

# Utilities
aiofiles==23.2.1
httpx==0.27.2
tenacity==8.5.0
tqdm==4.66.5

# Testing
pytest==8.3.2
pytest-asyncio==0.24.0
